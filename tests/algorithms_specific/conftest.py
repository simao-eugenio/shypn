# This directory contains integration/manual scripts, not pytest unit tests.
# Some files use sys.exit() and reference deleted module shypn.data.pathway.hierarchical_layout.
collect_ignore_glob = ["*.py"]
