% ============================================================================
% Title: Stochastic Thermodynamics of Single-Cell Commitment:
%        A Non-Equilibrium Analysis of Bacillus subtilis Sporulation
% Author: Eugênio Simão
% Status: working draft (July 2026)
% Target: Physical Review X / eLife (physics) / PRE
%
% This version frames the work explicitly around NESM + SHPN.
% Biology (Fujita constraints) is the validation prerequisite, not the
% main result. The main result is the thermodynamic characterisation of
% single-cell commitment as an open non-equilibrium system.
%
% Structure plan: workspace/projects/thesis/manuscript/parts/NESM_manuscript_plan.tex
% ============================================================================

\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{colortbl}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{setspace}
\usepackage[right]{lineno}

\geometry{margin=2.5cm}
\hypersetup{colorlinks=true,linkcolor=blue!50!black,
            citecolor=blue!50!black,urlcolor=blue!50!black}

\crefname{figure}{Fig}{Figs}
\Crefname{figure}{Fig}{Figs}

% ── Math macros ──────────────────────────────────────────────────────────────
\newcommand{\kB}{k_{\mathrm{B}}}
\newcommand{\Gs}{G_s}
\newcommand{\Ge}{G_E}
\newcommand{\thetaeff}{\theta_{\mathrm{eff}}}
\newcommand{\DeltaF}{\Delta F}
\newcommand{\CV}{\mathrm{CV}}
% EPR (entropy production rate)
\newcommand{\EPR}{\dot{\sigma}}
% Trajectory EPR
\newcommand{\TrajEPR}[1]{\sigma[#1]}
% Thermodynamic force / affinity
\newcommand{\aff}{\mathcal{A}}
% Topological load
\newcommand{\etaL}{\eta_{\mathrm{L}}}
% Critical exponent
\newcommand{\gsub}{\gamma_{\mathrm{sub}}}
% Critical nutrient
\newcommand{\Nc}{N_{\mathrm{c}}}
% Time-reversed trajectory
\newcommand{\trev}[1]{\tilde{#1}}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}
\doublespacing
\linenumbers
\vspace*{0.2in}

% ── Title and author ─────────────────────────────────────────────────────────
\begin{flushleft}
{\Large
\textbf{Stochastic Thermodynamics of Single-Cell Commitment:
A Non-Equilibrium Analysis of \textit{Bacillus subtilis} Sporulation}
}
\newline
\\
Eug\^{e}nio Sim\~{a}o$^{1,*}$
\\
\bigskip
\textbf{1} Department of Computer Science, Universidade Federal de Santa
Catarina (UFSC), Ararangu\'a, Santa Catarina, 88906-072, Brazil
\\
\bigskip
* eugenio.simao@ufsc.br
\end{flushleft}

% ─────────────────────────────────────────────────────────────────────────────
\section*{Abstract}
% ─────────────────────────────────────────────────────────────────────────────

Cells commit irreversibly to differentiation programmes — sporulation,
apoptosis, cell-cycle entry — yet the thermodynamic cost and universality
class of such decisions have never been characterised at the single-cell
level.
Classical thermodynamics is inapplicable to small, far-from-equilibrium
biological systems; stochastic thermodynamics (Seifert 2012), which
provides exact trajectory-level entropy production for Markov chains of
any size, offers a principled alternative.
Here we introduce the Signal Hierarchical Petri Net (SHPN) formalism as the
computational substrate for single-cell non-equilibrium statistical
mechanics (NESM).
SHPN encodes a biological principle that underlies all known commitment
circuits: the \emph{decision} is stochastic (low-copy regulatory
molecules; noise is the mechanism) and the \emph{execution} is
continuous (high-copy effectors; ODE regime).
Its arc semantics map directly onto the thermodynamic
ports of an open driven system, and five measurable thermodynamic
quantities --- topological load $\etaL$, dimensionless work $W/\kB T$,
critical nutrient level $\Nc$, sub-critical exponent $\gsub$, and
per-transition entropy production rate (EPR) decomposition --- are
derivable from the network topology and stochastic dynamics alone.
Applied to \textit{Bacillus subtilis} sporulation, the framework
reproduces three independent experimental constraints from Fujita \&
Losick (2005) and reveals that the commitment transition is
non-mean-field ($\gsub = 0.150 \pm 0.044$, 19$\sigma$ below $\gsub^{\mathrm{MF}}
= 0.5$), the critical threshold lies at $\Nc = 1346\,\mu$M, and 97.5\%
of the stochastic EPR is concentrated at the sensing step (KinA
autophosphorylation), not the execution programme.
These results establish SHPN as a natural language for stochastic
thermodynamics of regulatory decision circuits and provide a framework
applicable to any commitment network with known topology.
\vspace{0.5em}
\noindent\textit{Keywords:} stochastic thermodynamics, non-equilibrium
statistical mechanics, Petri nets, sporulation, critical phenomena,
entropy production, single-cell biophysics.

% ─────────────────────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}
% ─────────────────────────────────────────────────────────────────────────────

\subsection*{The thermodynamic gap in cell-fate decisions}

Living cells make irreversible commitment decisions that are the
foundation of development, homeostasis, and stress response:
sporulation, apoptosis, cell-cycle entry, stem-cell differentiation.
These are binary, largely irreversible transitions driven by intracellular
regulatory networks operating at single-molecule copy numbers.
Despite decades of molecular characterisation, no general thermodynamic
framework exists for quantifying the cost, precision, or universality class
of such decisions at the level of a single cell.

Classical thermodynamics cannot fill this gap.
It requires the thermodynamic limit ($N \to \infty$) and assumes
proximity to equilibrium.
A single cell violates both: key regulatory molecules involved in
commitment decisions --- the KinA sensor kinase, the Spo0A response
regulator, the SinR master repressor --- are present in tens to hundreds
of copies, precisely the regime where fluctuations are the mechanism
rather than noise to be averaged away.
Moreover, the cell is an open system far from equilibrium, maintained on
a non-equilibrium steady state (NESS) by constant ATP hydrolysis and
nutrient flux~\cite{Schnakenberg1976,Hill1977}.

Stochastic thermodynamics~\cite{Seifert2012} resolves this impasse.
Built on the exact result that entropy production along a single
stochastic trajectory is $\TrajEPR{\omega} = \ln P[\omega]/P[\trev\omega]$
--- the log-ratio of a trajectory's probability to its time-reversal ---
the formalism is exact for Markov chains of any size and has been
experimentally validated at the single-molecule
level~\cite{Liphardt2002,Wang2002,Crooks1999,Jarzynski1997}.
Applied to driven chemical reaction networks (CRNs)~\cite{Rao2018},
it provides per-flux dissipation budgets and connects network topology
to thermodynamic observables.
What has been missing is a computational substrate that can represent the
\emph{hybrid} nature of regulatory circuits --- simultaneously ODE-regime
(high-copy gene expression) and stochastic-regime (low-copy commitment
switches) --- within a single, formally grounded model.

\subsection*{SHPN as a unified substrate}

We propose the Signal Hierarchical Petri Net (SHPN)~\cite{Simao2025} as
this substrate.
SHPN is built around a biological principle that appears to be universal
across commitment circuits: \textbf{the decision is stochastic; the
execution is continuous.}
Commitment events are driven by regulatory molecules present at copy numbers
of tens to hundreds per cell (KinA, Spo0A$\sim$P, SinR, CI repressor,
CtrA) --- the regime where Poisson fluctuations are the mechanism, not
noise to be suppressed.
Execution programmes operate at thousands to tens-of-thousands of molecules
per cell ($\sigma$-factor cascades, coat proteins, caspases) --- the
ODE regime where fluctuations are negligible.
A formalism that conflates these two regimes --- modelling commitment with
ODEs or execution with stochastic rates --- will systematically
mis-attribute the thermodynamic dissipation budget.
SHPN is a 13-tuple Bio-PN formalism in which:
continuous ODE transitions model high-throughput processes ($\sigma$-factor
transcription, coat synthesis);
stochastic $\tau$-leaping transitions model low-copy decision events
(KinA activation, Spo0A phosphorylation, septation);
and signal-flow arcs encode regulatory hierarchy with a defined
thermodynamic semantics (the basin floor $\thetaeff$ is the minimum
dissipation threshold for downstream cascade activation).
Because stochastic SHPN transitions are Poisson approximations to the
Master equation of a continuous-time Markov chain (CTMC), the full
apparatus of stochastic thermodynamics applies to the decision layer
without approximation.
The ODE layer models the thermodynamic limit of the execution programme.

\subsection*{B. subtilis sporulation as working model}

\textit{Bacillus subtilis} sporulation is the paradigm case for
single-cell commitment.
The decision is binary and irreversible under nutrient starvation;
it is driven by a well-characterised phosphorelay cascade
(KinA $\to$ Spo0F $\to$ Spo0A $\sim$ P) gated by a master repressor
(SinR/SinI);
and it is physiologically stochastic --- only $\sim$38\% of a clonal
population sporulates under intermediate nutrient conditions, a
bet-hedging strategy that ensures population survival under fluctuating
environments~\cite{Fujita2005,Losick2008}.
Three quantitative constraints from Fujita \& Losick~\cite{Fujita2005}
--- the route independence of commitment, the $\sigma_H$ requirement, and
the nutrient threshold --- serve as an experimental falsification test
for the model before thermodynamic analysis is applied.

\subsection*{Here we show}

We construct a v9 SHPN model of \textit{B.~subtilis} sporulation (41
places, 36 transitions, 114 arcs), validate it against the Fujita
constraints, and then apply stochastic thermodynamics to compute the five
thermodynamic quantities introduced above.
We find that the commitment transition is \emph{non-mean-field}
($\gsub = 0.150 \pm 0.044$, $R^2 = 0.694$, $n = 7$), that the critical
nutrient threshold is $\Nc = 1346\,\mu$M, and that the thermodynamic cost
of commitment is overwhelmingly concentrated at the sensing step.
We discuss the physical origin of the non-mean-field universality class,
the scope and limitations of the Markovian reservoir assumption, and the
generality of the framework to other commitment circuits.

% ─────────────────────────────────────────────────────────────────────────────
\section{Theory I: The Single Cell as an Open Non-Equilibrium System}
\label{sec:nesm}
% Note: this section establishes the physical foundations. Readers primarily
% interested in the computational results may proceed to \Cref{sec:results_bio}
% and return here for derivations.
% ─────────────────────────────────────────────────────────────────────────────

\subsection{Non-equilibrium steady states and the living cell}

A living cell is the canonical example of an open non-equilibrium system.
It maintains steep electrochemical gradients, synthesises macromolecules
against entropic costs, and hydrolyses ATP at rates orders of magnitude
above what thermal fluctuations would produce at equilibrium.
These processes collectively define a \emph{non-equilibrium steady state}
(NESS): a stationary probability distribution $P_{\mathrm{ss}}(\mathbf{x})$
over molecular configurations $\mathbf{x}$ that is sustained by
continuous energy input from chemical reservoirs (nutrient, ATP/ADP/P$_i$).

At the NESS, probability currents circulate in state space --- a direct
manifestation of broken detailed balance.
The total entropy production rate is
\begin{equation}
  \EPR = \kB \sum_\rho \left(J_\rho^+ - J_\rho^-\right)
         \ln \frac{J_\rho^+}{J_\rho^-} \;\geq\; 0
  \label{eq:epr_def}
\end{equation}
where $J_\rho^\pm$ are the forward and reverse fluxes of reaction $\rho$.
Equality holds only when all currents vanish, i.e.\ at equilibrium.
This expression is derived from the Master equation without approximation
and applies to systems of any size~\cite{Schnakenberg1976,Seifert2012}.

\subsection{Stochastic thermodynamics: entropy production along a trajectory}

For a system described by a continuous-time Markov chain (CTMC) with
state space $\Omega$ and transition rates $k(\mathbf{x} \to \mathbf{x}')$,
stochastic thermodynamics assigns a thermodynamic weight to each
individual trajectory $\omega = (\mathbf{x}_0, t_1, \mathbf{x}_1, \ldots,
\mathbf{x}_n)$~\cite{Seifert2005,Seifert2012}.

Three conditions must hold:
\begin{enumerate}
  \item \textbf{CTMC} --- the system is Markovian (memoryless transitions).
  \item \textbf{Local detailed balance} --- for each reaction $\rho$,
    the ratio of forward to reverse rate encodes the free energy difference:
    \begin{equation}
      \frac{k^+_\rho}{k^-_\rho} = \exp\!\left(\frac{\Delta\mu_\rho}{\kB T}\right)
      \label{eq:ldb}
    \end{equation}
    where $\Delta\mu_\rho$ is the chemical potential difference driven by
    the reservoirs.
  \item \textbf{Explicit driving} --- the reservoirs that maintain the
    NESS (nutrients, ATP/ADP/P$_i$) are included in the model.
\end{enumerate}

Under these conditions, the entropy production along trajectory $\omega$
is
\begin{equation}
  \TrajEPR{\omega} = \ln \frac{P[\omega]}{P[\trev\omega]}
  \label{eq:traj_epr}
\end{equation}
where $\trev\omega$ is the time-reversed trajectory.
This is an exact identity --- not an approximation.
The ensemble average recovers \cref{eq:epr_def}: $\langle \TrajEPR{\omega}
\rangle = \EPR \cdot \tau$ for observation time $\tau$.
The Second Law at the trajectory level becomes $\langle \TrajEPR{\omega}
\rangle \geq 0$, with equality only at equilibrium.

Crucially, \cref{eq:traj_epr} is valid for $N = 10$ molecules as well as
$N = 10^{23}$.
This is the result that makes single-cell thermodynamics rigorous rather
than metaphorical~\cite{Liphardt2002,Wang2002}.

\subsection{Subsystem thermodynamics and the Markovian reservoir assumption}
\label{sec:subsystem}

A full cellular model would encompass thousands of concurrent processes.
We model only the sporulation commitment subnetwork ($k \ll N$ processes).
For such a \emph{subsystem}, Esposito \& Van den Broeck~\cite{Esposito2010}
showed that the EPR decomposes as
\begin{equation}
  \EPR_{\mathrm{total}} = \EPR_{\mathrm{sub}}
                        + \EPR_{\mathrm{rest}}
                        + \EPR_{\mathrm{interface}}
  \label{eq:epr_decomp}
\end{equation}
The \emph{interface term} captures correlations between the subnetwork
and the environment.
It vanishes exactly at the NESS when the coupling is \textbf{Markovian}:
when the rest of the cell acts as a memoryless reservoir on the timescale
of the commitment decision.

In the SHPN model, the rest of the cell enters through two explicit ports:
\begin{itemize}
  \item \textbf{ATP pool} (5000\,$\mu$M) --- a chemical bath whose
    chemical potential difference $\Delta\mu_{\mathrm{ATP}}
    = \kB T \ln([\mathrm{ATP}]/[\mathrm{ADP}][\mathrm{P}_i])$
    drives the phosphorelay cascade.
  \item \textbf{Nutrient level} ($N_0$) --- the input to KinA
    autophosphorylation.
\end{itemize}
The Markovian assumption holds when:
the metabolic correlation time of the cell ($\tau_{\mathrm{met}}$) is
short compared to the commitment timescale ($\tau_{\mathrm{commit}}
\approx 300$\,min);
and when no history-dependent feedback from unmodelled processes
reaches the commitment layer on that timescale.
We verify the assumption empirically: the model reproduces three
independent experimental constraints (Q1/Q2/Q3; \Cref{sec:results_bio})
without modelling any unmodelled process, confirming weak coupling.

\paragraph{Scope boundary.}
The five quantities characterise the sporulation commitment circuit
\emph{during the decision window} under starvation conditions.
They do not represent the total cellular EPR (dominated by translation
and replication), nor the full lifecycle thermodynamics.
This precision of scope is analogous to computing the thermodynamics of
kinesin without modelling the cytoplasm --- the motor's thermodynamics are
self-contained once the ATP reservoir and thermal bath are specified.

% ─────────────────────────────────────────────────────────────────────────────
\section{Theory II: SHPN as Computational Substrate for Single-Cell NESM}
\label{sec:shpn}
% ─────────────────────────────────────────────────────────────────────────────

\subsection{The 13-tuple Bio-PN formalism}

A Signal Hierarchical Petri Net is the 13-tuple
\begin{equation}
  \mathrm{SHPN} = (P,\, T,\, F,\, W,\, M_0,\, \Phi,\, C,\,
                   F_t,\, \Psi,\, F_s,\, W_s,\, \lambda,\, \theta)
  \label{eq:shpn_tuple}
\end{equation}
where: $P$ = places (molecular species / structural states);
$T$ = transitions (reactions / regulatory events);
$F$ = normal arcs (stoichiometric flows);
$W$ = arc weights;
$M_0$ = initial marking;
$\Phi$ = rate functions;
$C: P \to \text{Compartments}$ = compartment assignment;
% [NOTE: C does not appear in Enabled or Firing — but sets chemical potentials via mu_p = mu_0 + kT ln(M/(NA V_C(p)))]
% C is therefore a thermodynamic boundary operator (Note 13), not merely a simulation-level parameter.
$F_t \subseteq (P\times T)$ = test arcs (non-consuming presence checks);
$\Psi \subseteq P$ = signal places (regulatory hubs);
$F_s \subseteq (\Psi \times T) \cup (T \times \Psi)$ = signal-flow arcs
(consuming, cascade-gated);
$W_s$ = signal arc weights;
$\lambda$ = layer assignment (POSet of cascade depth);
$\theta$ = basin floors per transition (from the $\Gamma = (K,n,\varepsilon)$
Hill tuple).

Enablement of a transition $t$ under marking $M$ requires all four
conditions:
\begin{align}
  \mathrm{Enabled}(t,M) &\equiv
    \mathrm{NormalEnabled}(t,M) \nonumber \\
  &\;\wedge\; \mathrm{TestEnabled}(t,M) \nonumber \\
  &\;\wedge\; \mathrm{SignalEnabled}(t,M)
    \quad [M(p_s) \geq \thetaeff(t) + W_s((p_s,t))] \nonumber \\
  &\;\wedge\; \mathrm{PreemptionCheck}(t,M)
  \label{eq:enablement}
\end{align}
Full firing semantics and proofs are in Supplementary~S1.

\subsection{Why SHPN maps to stochastic thermodynamics}
\label{sec:shpn_nesm_map}

The stochastic transitions of the SHPN are governed by $\tau$-leaping:
at each step $\Delta t$, transition $\rho$ fires $k_\rho \sim
\mathrm{Poisson}(\Phi_\rho(M) \cdot \Delta t)$ times.
This is a Poisson approximation to the exact Master equation of a CTMC
with propensity $\Phi_\rho(M)$ and stoichiometry $\Delta M_\rho$.
The approximation error is $O((\Delta t)^2)$ and vanishes in the limit
$\Delta t \to 0$~\cite{Gillespie2001}.

Three consequences follow:
\begin{enumerate}
  \item The three conditions for stochastic thermodynamics
    (\Cref{sec:nesm}) are satisfied by construction for the stochastic
    layer: CTMC (condition 1), local detailed balance encoded in rate
    functions (condition 2), explicit reservoirs (condition 3).
  \item The trajectory EPR (\cref{eq:traj_epr}) is computable directly
    from simulation output: $\TrajEPR{\omega} = \sum_\rho k_\rho \ln
    (\Phi_\rho^+ / \Phi_\rho^-)$ summed over observed firing events.
  \item The ODE transitions correspond to the deterministic limit
    $N \to \infty$ of the same Master equation.
    Their EPR is the continuous-flow limit:
    $\EPR_{\mathrm{ODE}} = \sum_\rho J_\rho \ln(J_\rho^+/J_\rho^-)$.
\end{enumerate}

The hybrid formalism therefore separates the cell into two thermodynamic
regimes with a single consistent substrate:
\begin{description}
  \item[Layer A (stochastic, low-copy)] the commitment decision layer
    --- KinA, Spo0F/Spo0A phosphorelay, SinI/SinR gate, septation.
    Fluctuations are the biological mechanism; EPR is trajectory-level.
  \item[Layer B (continuous, high-copy)] the execution programme ---
    $\sigma$-factor transcription cascades, coat synthesis, maturation.
    Mean-field ODE limit; EPR is flux-affinity product.
\end{description}

\subsection{Arc semantics as thermodynamic ports}

Each arc type in the SHPN plays a distinct thermodynamic role:

\begin{description}
  \item[Normal arc] Stoichiometric flow; contributes mass transfer
    $\Delta M = W$ per firing and full EPR per event.
  \item[Signal-flow arc] Regulatory dissipation channel; consumptive
    ($\Delta M = W_s$), cascade-gated by $\thetaeff$.
    The basin floor $\thetaeff$ is the minimum token count at the signal
    place required for the downstream programme to activate --- a
    topological encoding of the minimum dissipation threshold.
  \item[Test arc] Zero-mass-transfer sensing ($\Delta M = 0$);
    EPR contribution is zero for the arc, but gates transitions that
    do dissipate.
    Analogous to a catalyst: participates in the decision without
    entering the energy balance.
  \item[Inhibitor arc] Presence-check with threshold; $\Delta M = 0$,
    disables the transition when $M(p) \geq \thetaeff$.
    Encodes a free energy barrier without consuming tokens.
\end{description}

The arc graph therefore encodes the \emph{thermodynamic topology} of the
commitment circuit: which flows carry mass (and thus contribute EPR),
which carry only information (EPR-neutral), and which constrain the
basin floor.

\paragraph{The POSet $\lambda$ as an entropic ratchet.}\footnote{We use
``entropic ratchet'' in the loose sense of a directed free-energy-consuming
cascade, distinct from a Brownian ratchet~\cite{Ajdari1992,Magnasco1993},
where spatial asymmetry rectifies thermal fluctuations into directed motion.
The directionality in the POSet cascade arises from accumulated chemical
potential differences ($\sum_k \Delta\mu_k \gg k_BT$), not from spatial
asymmetry of a fluctuating potential.
The ratchet concept is most precise at the commitment moment itself
(near $\Nc$), where a stochastic SinI burst --- a genuine thermal
fluctuation --- is rectified into an irreversible fate decision by
the asymmetric free energy landscape of the downstream execution
programme.}
The layer assignment $\lambda$ imposes a directed state space in which
each level transition is irreversible because the free energy cost of
ascending is consumed from the chemical bath and cannot be recovered.
For a cascade $a \to b \to c$ ($n$ levels), the forward-to-reverse ratio
of the full cascade is:
\begin{equation}
  \frac{W(0 \to n)}{W(n \to 0)}
  = \exp\!\left(\frac{\mathcal{A}_{\mathrm{total}}}{\kB T}\right)
  = \exp\!\left(\frac{\sum_{k=0}^{n-1} \Delta\mu_k}{\kB T}\right)
  \label{eq:ratchet}
\end{equation}
With $\mathcal{A}_{\mathrm{total}} = W/\kB T = 3.72\times10^7$, the
ratio is $\approx 10^{16{,}000{,}000}$.
PreemptionCheck reinforces this: the reverse trajectory $\tilde\omega$
requires $b$ to fire before $a$, but $\Phi_b(M_{\mathrm{before}\,a}) = 0$
(PreemptionCheck fails), so $P[\tilde\omega] = 0$ under forward dynamics.

This raises a legitimate theoretical concern: setting a propensity to
exactly zero is a biochemical constraint, not a thermodynamic consequence.
In canonical stochastic thermodynamics every reaction has a non-zero
reverse rate; irreversibility emerges from the magnitudes of affinities,
not from imposed architectural rules.
PreemptionCheck should therefore be understood as the idealised limit of
strong kinetic suppression ($k^-_k \to 0$ as protein-protein specificity
becomes perfect), representing the biochemical fact that the phosphorelay
is causally ordered by molecular recognition --- Spo0F cannot be
phosphorylated without KinA~P present, not merely because it is
energetically unfavourable but because the substrate is absent.
Biology achieves commitment irreversibility through two independent
mechanisms acting in concert: \emph{thermodynamic driving}
($\Delta\mu_k \gg k_BT$ at each ATP-consuming step, encoded in rate
functions via local detailed balance) and \emph{biochemical causal
ordering} (protein specificity, encoded by PreemptionCheck).
Neither alone is sufficient; together they make reversal impossible at
both the energetic and the kinetic level.
\emph{Biological commitment irreversibility is therefore doubly enforced:
thermodynamically by the accumulated affinity of the cascade, and
biochemically by the protein-specificity constraints that make the
reverse pathway a different molecular process entirely.}

\subsection{Compartment assignment as thermodynamic boundary specification}
\label{sec:C_thermodynamic}

The compartment assignment $C: P \to \text{Compartments}$ assigns each
place $p$ to a physical volume $V_{C(p)}$.  This geometric tag enters
stochastic thermodynamics at three levels.

\paragraph{Chemical potential.}
The chemical potential of species $p$ is
\begin{equation}
  \mu_p = \mu_p^\circ + k_BT \ln\!\left(\frac{M(p)}{N_A \cdot V_{C(p)}}\right).
  \label{eq:C_chempot}
\end{equation}
The same token count $M(p)$ yields a different $\mu_p$ in each compartment,
so $C$ determines the thermodynamic driving force of every transition that
consumes $p$.  It is not neutral bookkeeping.

\paragraph{Local detailed balance and EPR.}
For a transition spanning two compartments ($C(p_{\mathrm{in}}) \neq
C(p_{\mathrm{out}})$), local detailed balance gives
$k_t^+ / k_t^- = \exp(-\Delta G_t / k_BT)$, where $\Delta G_t = \sum_i
\nu_i \mu_i$ depends on the volumes via~\cref{eq:C_chempot}.  A
cross-compartment transition therefore has $\Delta G_t \neq 0$ at equal token
counts, producing non-zero EPR by construction --- compartment boundaries are
the \emph{structural origin of irreversibility} in the formalism.

\paragraph{Septation changes $C$ at runtime.}
The septation event ($T_{\text{septation}}$) is the transition that
re-assigns $C$ for forespore places, creating the forespore/mother-cell
thermodynamic boundary.  Before septation, $\sigma^F$ and $\sigma^E$ share a
common volume; after, they are thermodynamically isolated.  The Waddington
basin boundary $M_{\text{commit}}$ is exactly the token count at which
$\Delta G_{\text{septation}} < 0$ becomes irreversible --- the
free-energy landscape changes curvature from double-well to single-well at
this point.  See Supplementary Note~13 for the full derivation.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{The five topological-thermodynamic quantities}
\label{sec:five_quantities}

\paragraph{Quantity 1 --- Topological load $\etaL$.}
\begin{equation}
  \etaL = \frac{|\Aset_{\mathrm{commit}}|}{|P_{\mathrm{commit}}|
               \cdot |T_{\mathrm{commit}}|}
  \label{eq:eta_L}
\end{equation}
Arc density of the commitment layer $G_s$.
Measures informational load per place-transition pair.
Derivable from the arc graph alone; no simulation required.

\paragraph{Quantity 2 --- Dimensionless work $W/\kB T$.}
Cumulative dissipative work along the commitment trajectory:
\begin{equation}
  \frac{W}{\kB T} = \int_0^{\tau_c} \frac{\EPR(t)}{\kB T}\, \mathrm{d}t
  \label{eq:W_kBT}
\end{equation}
When $W/\kB T \gg 1$, the Fluctuation Theorems of Jarzynski~\cite{Jarzynski1997}
and Crooks~\cite{Crooks1999} are inapplicable (the system is deep in
the non-linear NESS regime).
This is a result about the commitment process, not a limitation of the
method.

\paragraph{Quantity 3 --- Critical nutrient level $\Nc$.}
The nutrient concentration at which sporulation probability crosses 50\%.
Operationally, $\Nc$ is the order-parameter transition point of the
commitment landscape: below $\Nc$, stochastic fluctuations drive the
cell toward the sporulation attractor; above $\Nc$, the vegetative
attractor dominates.

\paragraph{Quantity 4 --- Sub-critical exponent $\gsub$.}
Near $\Nc$, the binary coefficient of variation diverges as
\begin{equation}
  \CV^2_{\mathrm{bin}} \;\sim\; |N_0 - \Nc|^{-\gsub}
  \label{eq:gsub}
\end{equation}
The mean-field prediction is $\gsub^{\mathrm{MF}} = 0.5$ (equivalent to
a Bernoulli random variable near its critical point).
Deviations indicate long-range correlations in the decision network ---
a non-trivial universality class.

\paragraph{Quantity 5 --- Per-transition EPR decomposition.}
\begin{equation}
  \EPR_{\mathrm{total}} = \sum_{t \in T_{\mathrm{stoch}}} \EPR_t
                        + \sum_{t \in T_{\mathrm{ODE}}} \EPR_t
  \label{eq:epr_decomp2}
\end{equation}
Identifies which transitions dominate the thermodynamic cost of the
decision and how the dissipation budget evolves over the commitment
trajectory.

% ─────────────────────────────────────────────────────────────────────────────
\section{Methods}
\label{sec:methods}
% ─────────────────────────────────────────────────────────────────────────────

\subsection{SHPN model of \textit{B. subtilis} sporulation}

The v9 model (41 places, 36 transitions, 114 arcs) is described in full
in Supplementary~S2.
In brief, it contains:
\begin{itemize}
  \item 7 stochastic transitions forming the commitment layer (Layer A):
    KinA activation, Spo0F/Spo0A phosphorylation and dephosphorylation,
    septation, and SinI synthesis.
  \item 22 ODE transitions forming the execution layer (Layer B):
    $\sigma$-factor transcription cascades ($\sigma_H$, $\sigma_F$,
    $\sigma_E$, $\sigma_G$, $\sigma_K$), coat assembly, and maturation.
  \item Token unit: 1\,token $= 1\,\mu$M.
    ATP pool $M_0 = 5000\,\mu$M; nutrients $M_0 = 1440\,\mu$M (wild-type).
  \item Key threshold: SinR inhibitor arc on T\_septation with
    $\thetaeff = 7.5\,\mu$M.
\end{itemize}

\begin{figure}[p]
  \centering
  {\large\textbf{SHPN v9 model of \textit{B.~subtilis} sporulation (corrected)}}
  \par\vspace{6pt}
  \includegraphics[width=\linewidth]{figures/bacillus_sporulation_v9_titled.pdf}
  \caption{\textbf{SHPN v9 model of \textit{B.~subtilis} sporulation.}
  Circles: biological places ($\mu$M).
  Rectangles: transitions (stochastic = dashed border; ODE = solid).
  Hexagons: signal places ($\Psi$).
  Arc styles: solid black = normal; dashed blue = test;
  dashed grey = signal-flow; filled-circle head = inhibitor.
  Layer~A (commitment, stochastic, upper-left cluster) and Layer~B
  (execution, ODE, right) are structurally separated in the arc graph.
  Four enzyme-regeneration arcs (KinA/Spo0F/Spo0A recycling steps) are
  shown as solid black (normal arcs) consistent with the corrected model
  in which G_s is acyclic.}
  \label{fig:model}
\end{figure}

\subsection{Simulation protocol}

All simulations used the SHPN $\tau$-leaping engine
($\Delta t = 0.01$\,s, $\tau_{\mathrm{max}} = 0.01$\,s to suppress
Poisson over-sampling of low-copy transitions).
Each condition used $n = 50$ stochastic replicates over a 6-hour
horizon.
Sporulation was scored when at least one Mature\_spore token was
present at $t = 6$\,h.
EPR was computed per-replicate from firing counts using
\cref{eq:traj_epr}.

For the critical exponent analysis (\Cref{sec:results_thermo}), a
dense nutrient grid (1200--1550\,$\mu$M, 13 conditions) was used with
$n = 50$ replicates per condition.
$\CV^2_{\mathrm{bin}}$ was computed as $p(1-p)/p^2$ where $p$ is the
sporulation fraction; the fit used only sub-critical conditions
($N_0 < \Nc$) to avoid Bernoulli-dominated variance on the sup-critical
side.

\subsection{EPR computation}
\label{sec:methods_epr}

For each stochastic replicate trajectory $\omega$, the per-transition
EPR contribution at time $t$ is
\begin{equation}
  \EPR_t(\omega) = \frac{1}{\Delta t} \sum_{\rho \in t}
    k_\rho(\omega,t) \ln \frac{\Phi_\rho^+(M)}{\Phi_\rho^-(M)}
  \label{eq:epr_compute}
\end{equation}
where $k_\rho$ is the observed number of firings of elementary reaction
$\rho$ (forward or reverse) in $[t, t+\Delta t]$ and
$\Phi_\rho^\pm(M)$ are the propensities evaluated at the current marking.
This is the discrete-time approximation to \cref{eq:traj_epr}; the
error is $O(\Delta t)$.

Reversible reactions use the Skellam sampler
($k_{\mathrm{net}} \sim \mathrm{Skellam}(\Phi^+\Delta t, \Phi^-\Delta t)$)
and the log-ratio is evaluated at the net firing direction.
ODE-regime transitions contribute $\EPR_t = J_t \ln(J_t^+/J_t^-)
\cdot \Delta t$ where $J_t^\pm$ are the continuous fluxes.

% ─────────────────────────────────────────────────────────────────────────────
\section{Results I: Biological Validation}
\label{sec:results_bio}
% ─────────────────────────────────────────────────────────────────────────────

% Note: this section is the prerequisite for thermodynamic analysis.
% The subnetwork model must be biologically grounded before NESM quantities
% can be trusted. Three experimental constraints from Fujita & Losick (2005)
% are reproduced.

Before thermodynamic analysis, the SHPN model must be validated against
established experimental constraints.
Fujita \& Losick~\cite{Fujita2005} characterised three properties of
\textit{B.~subtilis} sporulation commitment that any model must
reproduce.
We address them in order.

\subsection{Q1: Commitment route and metabolic state (Fujita Fig.\ 3)}

Sweep~F1 tested a $2\times2$ factorial of medium ($N_0 = 100\,\mu$M, fast
starvation vs.~$N_0 = 1440\,\mu$M, slow starvation) crossed with
commitment route (natural phosphorelay vs.~abrupt Spo0A$\sim$P pulse,
$\texttt{LOADING\_DOSE} = 27\,\mu$M).
In SM-like conditions ($N_0 = 100$) both routes gave $100\%$ sporulation;
the abrupt pulse merely shortened mean commitment time (151$\to$136~min).
In CH-like conditions ($N_0 = 1440$) the natural route gave $38\%$ and the
abrupt pulse $56\%$ --- neither reached the SM value.
The thermodynamic explanation is direct: SM medium drives the ATP pool below
the commitment floor of $1.96$~mM; CH medium sustains ATP above this
floor throughout the 6-h window, so KinA activation --- gated by
$[1 + (\text{Nutrients})^2]^{-1}$ --- remains suppressed.
The commitment route is a secondary modulator; the nutrient-depletion
trajectory is the primary gate.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig2_q1_gate.png}
  \caption{\textbf{Q1: nutrient level gates commitment; ATP trajectory confirms the mechanism.}
  \textbf{(a)} Sporulation fraction (\%) vs.\ initial nutrient level $N_0$ (FUJITA-2,
  abrupt Spo0A* pulse fixed, $n = 100$ replicates/condition; Sweep~F2,
  run~\texttt{20260702\_180944}).
  Fraction decreases monotonically from $100\%$ at $N_0 = 100\,\mu$M to
  $0\%$ at $N_0 > 1800\,\mu$M; dotted line: 50\% crossover at
  $N_c \approx 1440$--$1600\,\mu$M.
  \textbf{(b)} ATP pool trajectories under SM-equivalent ($N_0 = 100\,\mu$M, red)
  and CH-equivalent ($N_0 = 1440\,\mu$M, green) conditions, natural phosphorelay
  route ($n = 100$; mean $\pm$ SD; FUJITA-1, run~\texttt{20260702\_171823}).
  SM depletes ATP below the $1.96$~mM commitment floor within 30~min, triggering
  commitment; CH sustains ATP above the floor throughout the 6-h horizon.}
  \label{fig:q1}
\end{figure}

\subsection{Q2: $\sigma_H$ is the irreversible gate (Fujita Fig.\ 4)}

Sweep~F3 titrated the $\sigma_H$ transcription rate via a scaling parameter
$k_{\sigma H} \in \{0, 0.2, 1.0\}$ at fixed SM nutrients ($N_0 = 100\,\mu$M).
At $k_{\sigma H} = 0$ (true $\sigma_H$-null), sporulation was $0\%$ regardless
of whether the abrupt Spo0A$\sim$P pulse was applied or not --- the pulse
cannot rescue commitment in the absence of $\sigma_H$.
At $k_{\sigma H} = 0.2$ sporulation recovered to $96$--$100\%$;
wild-type ($k_{\sigma H} = 1.0$) gave $100\%$ with mean peak
$[\sigma_H] \approx 1.64\,\mu$M (above $\theta_{\sigma H} = 1.60\,\mu$M).
The null/active transition is a step function, not a gradient.
This confirms that $\sigma_H$ is not a downstream reporter of commitment
but the irreversible gate: it is the sole transition in the SHPN at
which the execution layer ($\sigma_F$, $\sigma_E$, $\sigma_G$, $\sigma_K$
cascades) becomes enabled.
Thermodynamically, $\sigma_H$ is where $97.5\%$ of the stochastic EPR
is paid (\Cref{sec:results_epr}); removing it eliminates the only step
at which the circuit pays its dissipation budget.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig3_q2_sigmaH.png}
  \caption{\textbf{Q2: $\sigma_H$ is the irreversible commitment gate.}
  (Left) Sporulation fraction vs.\ $k_{\sigma H}$ for natural (blue) and
  abrupt (orange) routes; step-like null/active transition.
  (Right) $\sigma_H$ trajectory at wild-type showing stochastic crossing
  of the $\theta_{\sigma H} = 1.60\,\mu$M threshold (dashed).
  Sweep F3, run~\texttt{20260702\_191849}.}
  \label{fig:q2}
\end{figure}

\subsection{Q3: Nutrient threshold and commitment timing (Fujita Fig.\ 5)}

Sweep~F4 titrated $N_0$ from $100$ to $2000\,\mu$M (natural route,
200~replicates/condition).
Sporulation fraction decreases monotonically: $100\%$ at
$N_0 = 100\,\mu$M, $39\%$ at $1440\,\mu$M, $0\%$ at $2000\,\mu$M.
The effective threshold is $\Nc \approx 1370\,\mu$M (F4 coarse estimate;
refined to $1346\,\mu$M in the dense NET-T4 sweep, \Cref{sec:results_critical}).
The bet-hedging window spans $700$--$1800\,\mu$M, the range in which
neither the sporulation nor the vegetative attractor dominates.

Mean commitment time increases from $\sim 151$~min ($N_0 = 100$) to
$\sim 335$~min ($N_0 = 1440$): the delay is explained by the deterministic
nutrient-depletion clock ($t_{\mathrm{depl}} = N_0 / 0.1\,\mu$M~s$^{-1}$),
which sets the moment KinA activation becomes possible.
Cells at higher $N_0$ wait longer for nutrients to clear before the
phosphorelay can accumulate.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig4_q3_Nc.png}
  \caption{\textbf{Q3: nutrient threshold, critical slowing-down, and threshold-crossing mechanism.}
  \textbf{(a)} Sporulation fraction vs.\ $N_0$ (natural route, $n = 200$
  replicates/condition; FUJITA-4, run~\texttt{20260704\_163628}).
  Red dashed line: $\Nc \approx 1346\,\mu$M (50\% crossing);
  dotted line: 50\% reference.
  \textbf{(b)} Mean commitment time near $\Nc$ (dense sub-critical grid,
  $n = 300$ replicates/condition, 60 trajectories sampled;
  NET-T4, run~\texttt{20260707\_134537}).
  Commitment time rises from $\approx 310$~min to $\approx 350$~min as
  $N_0 \to \Nc$ — critical slowing-down signature of a second-order transition.
  \textbf{(c)} Mean $\sigma_H$ peak vs.\ $N_0$ near $\Nc$ (same dense sweep).
  All mean peaks remain \emph{below} the effective threshold
  $\theta_{\sigma H} = 1.60\,\mu$M (dotted line), confirming that sporulation
  is driven by stochastic threshold-crossing events, not by a mean-field
  crossing.}
  \label{fig:q3}
\end{figure}

% ─────────────────────────────────────────────────────────────────────────────
\section{Results II: Thermodynamic Characterisation of Commitment}
\label{sec:results_thermo}
% ─────────────────────────────────────────────────────────────────────────────

\subsection{Topological load $\etaL$ and commitment modes (NET-T1)}
\label{sec:results_eta}

The Landauer efficiency $\etaL \approx 1.82$--$1.88 \times 10^{-8}$ is
constant across all half-life conditions where sporulation occurs
(HL~$= 10$--$120$~min; ATP consumption varies by ${<}3\%$,
$2770$--$2860\,\mu$M per committing replicate, \Cref{tab:t1}).
Biological commitment switches are nowhere near the Landauer bound;
more importantly, the efficiency is \emph{set by the circuit topology},
not by kinetic parameters: the $\Gamma = (K,n,\varepsilon)$ Hill tuple
encodes the gate sharpness, and $\etaL$ scales with gate co-operativity.

\begin{table}[h]
\centering
\caption{Landauer efficiency $\etaL$ across $\sigma_H$ half-life conditions
(NET-T1; 100~reps/condition; $N_0 = 1440\,\mu$M;
run~\texttt{20260704\_181236}).}
\label{tab:t1}
\begin{tabular}{rrrr}
\toprule
HL (min) & Spor.\,\% & $N_{\mathrm{ATP}}$ ($\mu$M) & $\etaL$ \\
\midrule
20  & 100 & 2776 & $1.88\times10^{-8}$ \\
60  & 100 & 2858 & $1.82\times10^{-8}$ \\
90  & 100 & 2852 & $1.83\times10^{-8}$ \\
120 &  38 & 2860 & $1.82\times10^{-8}$ \\
\bottomrule
\end{tabular}
\end{table}

However, the sporulation fraction is non-monotonic in half-life: HL~$= 20$--$90$~min
gives $100\%$ (optimal transient amplifier window); wild-type HL~$= 120$~min gives
$38\%$ (bet-hedging fraction at $N_0 = N_c$); HL~$\geq 150$~min gives $0\%$
(SinR never drops below threshold despite $\sigma_H$ accumulating above it --- a
\emph{$\sigma_H$-locked vegetative state}).
The wild-type half-life is not optimised for efficiency (which is topology-constant)
but for maximum-entropy operating point of the noise-driven bifurcation landscape.

Trajectory analysis identifies two mechanistically distinct commitment modes
depending on half-life:
\begin{description}
  \item[Mode~A --- passive SinR drift (HL $\leq 90$~min).] SinR falls below
    $7.5\,\mu$M within $0.1$--$0.6$~min via natural continuous degradation,
    without SinI-mediated sequestration.
    Septum firing follows $\sim 244$~min later, set by the nutrient-depletion
    clock, independent of $\sigma_H$ level.
  \item[Mode~B --- active SinI/SinR (HL $= 120$~min, wild-type).] SinR
    remains above threshold until $t \approx 335$~min, when a stochastic
    SinI synthesis burst (driven by Spo0A$\sim$P) sequesters SinR below
    $7.5\,\mu$M.  Septum fires within $0.3$~min.
    Only $38\%$ of replicates achieve the SinI burst, directly producing
    the bet-hedging fraction.
\end{description}

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig5_thermo_cost.png}
  \caption{\textbf{Topological load and thermodynamic cost.}
  (Left) $\etaL$ vs.\ $\sigma_H$ half-life showing topology-constancy
  despite varying sporulation fraction.
  (Right) $W/\kB T$ bar chart; dashed line marks the Landauer bound.
  NET-T1/T3, runs~\texttt{20260704\_181236} and \texttt{20260706\_151050}.}
  \label{fig:thermo_cost}
\end{figure}

\subsection{Free energy landscape and dissipative work (NET-T2, NET-T3)}
\label{sec:results_landscape}

The effective potential $U_{\mathrm{eff}}(\varphi, t) = -\ln P_{\mathrm{ss}}(\varphi, t)$
was reconstructed from the distribution of the composite order parameter
$\varphi$ (integrating Spo0A$\sim$P, $\sigma_H$, and Septum trajectories)
across 100 replicates at each nutrient condition
(run~\texttt{20260704\_201120}).
This is a rigorously defined potential, not a cartoon: it is the
logarithm of the empirical steady-state distribution, exactly as defined
by stochastic thermodynamics~\cite{Seifert2012}.

Five trajectory-level features are notable:
(i) the nutrient-depletion clock is deterministic ($\sigma_{t_{\mathrm{depl}}} = 0$);
(ii) the ATP commitment floor is $1.96$~mM regardless of $N_0$ --- a
topology-derived constant;
(iii) the commitment fraction equals $P(\sigma_H^{\mathrm{stoch}} > \theta_{\sigma H})$
--- a noise-driven threshold-crossing, not a mean-field switch;
(iv) SinR fluctuates in a narrow near-threshold band ($7.28$--$7.45\,\mu$M,
only $0.05$--$0.22\,\mu$M below the $7.5\,\mu$M inhibitor threshold),
maximising sensitivity to stochastic SinI bursts;
(v) temporal priming: cells that deplete nutrients later commit faster,
because their phosphorelay pools are larger at starvation onset.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig6a_waddington.png}
  \caption{\textbf{Non-equilibrium Waddington landscape.}
  $U_{\mathrm{eff}}(\varphi, t) = -\ln P_{\mathrm{ss}}(\varphi, t)$
  at $N_0 = 1440\,\mu$M, reconstructed from 20 stochastic replicates.
  White line: mean $\varphi(t)$.
  Yellow circle: commitment crossing at $\thetaeff$ threshold.
  Green star: first mature spore.
  NET-T2, run~\texttt{20260704\_201120}.}
  \label{fig:waddington}
\end{figure}

A $2\times2$ protocol sweep (NET-T3) confirmed thermodynamic irreversibility.
Forward (FWD, $N_0 = 1440 \to 2160\,\mu$M at 1~h) and reverse (REV,
$2160 \to 1440\,\mu$M) perturbations give $W/\kB T = -2.67 \times 10^6$
(negative: nutrient enrichment produces ATP) and
$W/\kB T = +2.13 \times 10^7$ respectively.
The baseline $W/\kB T = +3.72 \times 10^7$.
At this scale the Jarzynski exponential average $\langle e^{-W/\kB T}\rangle$
underflows in 64-bit arithmetic ($e^{-W/\kB T} < 10^{-10^7}$):
the Jarzynski equality and Crooks fluctuation theorem are inapplicable.
This is a result, not a limitation: the circuit operates deep in the
non-linear NESS regime where equilibrium fluctuation theorems are expected
to fail~\cite{Jarzynski1997,Crooks1999}.
Biologically, FWD aborts sporulation completely ($38\% \to 0\%$); REV
only weakly triggers it ($0\% \to 2\%$): commitment is easy to prevent,
nearly impossible to induce by external perturbation alone.

\subsection{Non-mean-field critical transition near $\Nc$ (NET-T4)}
\label{sec:results_critical}

The sub-critical scaling of $\CV^2_{\mathrm{bin}}$ follows
\cref{eq:gsub} with fitted exponent $\gsub = 0.150 \pm 0.044$
($R^2 = 0.694$, $n = 7$ sub-critical conditions, $p < 10^{-6}$).
This is $19\sigma$ below the mean-field prediction $\gsub^{\mathrm{MF}} = 0.5$.

The critical threshold is $\Nc = 1346\,\mu$M, identified as the
nutrient level at which sporulation probability crosses 50\%.
Critical slowing-down is observed: the mean commitment time rises from
317\,min at $N_0 = 1250\,\mu$M to 337\,min at $N_0 = 1345\,\mu$M,
a 6.3\% increase over 95\,$\mu$M --- the characteristic divergence of the
relaxation time near a second-order transition.

Near $\Nc$, the $\sigma_H$ peak concentration remains below the
activation threshold $\theta_{\sigma H} = 1.60\,\mu$M, so the commitment
probability is $P(\mathrm{spor}) = P(\sigma_H > \theta_{\sigma H})$ ---
a threshold-crossing probability driven by stochastic fluctuations in
Layer~A, not a deterministic crossing.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig6b_critical_exponents.png}
  \caption{\textbf{Non-mean-field critical exponent at the commitment bifurcation.}
  Log-log plot of $\CV^2_{\mathrm{bin}}$ vs.\ $|N_0 - \Nc|$ for sub-critical
  conditions ($N_0 < \Nc = 1346\,\mu$M).
  Power-law fit gives $\gsub = 0.150 \pm 0.044$ ($R^2 = 0.694$, $n = 7$);
  dashed line: mean-field prediction $\gsub^{\mathrm{MF}} = 0.5$.
  Dense sub-critical sweep, run~\texttt{20260707\_134537}, 300~reps/condition.}
  \label{fig:critical}
\end{figure}

\subsection{Entropy production decomposition (NET-T5)}
\label{sec:results_epr}

Per-transition EPR was computed from firing counts using \cref{eq:epr_compute}
at three nutrient levels: sub-critical ($N_0 = 100\,\mu$M, $100\%$ sporulation),
critical ($1600\,\mu$M, $14\%$), and supra-critical ($2200\,\mu$M, $0\%$;
300~replicates/condition, run~\texttt{20260707\_152611}).

Four findings:

\textbf{KinA dominates stochastic EPR ($97.5\%$).}
KinA autophosphorylation fires $\sim 1000$ times per replicate over 6~h
(rate $4.67 \times 10^{-2}$~s$^{-1}$), each costing 5~ATP~$= 116.3\,\kB T$.
T\_septation fires once per sporulating cell but at rate
$4.63 \times 10^{-5}$~s$^{-1}$.
KinA therefore contributes $97.5\%$ of the stochastic EPR.
This quantifies the cost of \emph{continuous environmental sensing} by the
phosphorelay: the cell pays in ATP to integrate the nutrient signal at
every KinA cycle.

\textbf{Stochastic EPR tracks commitment probability.}
Total stochastic EPR decreases 6-fold from $5.6\,\kB T$/s ($N_0 = 100$)
to $0.9\,\kB T$/s ($1600\,\mu$M) to $\approx 0$ ($2200\,\mu$M).
The EPR is proportional to KinA activation level, which is proportional to
$P(\mathrm{commit}) = P(\sigma_H^{\mathrm{stoch}} > \theta_{\sigma H})$.
The circuit's thermodynamic output IS its information-processing output.

\textbf{Septation rate asymmetry: $14.3\times$ in sporulating vs.\ vegetative cells.}
At $N_0 = 1600\,\mu$M, T\_septation fires at $4.63 \times 10^{-5}$~s$^{-1}$
in sporulating replicates vs.\ $3.23 \times 10^{-6}$~s$^{-1}$ in vegetative
ones --- the strongest single EPR biosignature of the fate decision.

\textbf{Decision and execution channels are orthogonal.}
The stochastic EPR (decision layer, KinA-dominated) and the ODE EPR
(execution layer, $\sigma$-factor synthesis and coat assembly) are
measured in separate channels consistent with the $\Ge \times \Gs$
decomposition: noise is a resource in the decision layer and a liability
in the execution layer.

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{figures/fig7_epr.png}
  \caption{\textbf{Entropy production rate decomposition (NET-T5).}
  (Left) Stacked per-transition EPR bars at three nutrient conditions;
  KinA dominates at $97.5\%$.
  (Right) Septation rate asymmetry between sporulating and vegetative
  replicates at $N_0 = 1600\,\mu$M.
  Run~\texttt{20260707\_152611}, 300~replicates/condition.}
  \label{fig:epr}
\end{figure}

% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}
% ─────────────────────────────────────────────────────────────────────────────

\subsection{The physical origin of the non-mean-field universality class}

Mean-field theory ($\gsub = 0.5$) assumes that fluctuations at the
critical point are uncorrelated: each molecule makes an independent
binary decision, and the variance diverges as a Bernoulli process.
The phosphorelay cascade contradicts this assumption at every step.
KinA~$\to$ Spo0F~$\to$ Spo0A~$\sim$P is a three-step sequential amplifier:
a fluctuation in KinA activity propagates multiplicatively through
the chain, generating correlated excursions in all downstream species
simultaneously.
Furthermore, the SinR inhibitor arc on T\_septation introduces a
nonlinear threshold coupling: the decision does not activate until
SinI-mediated sequestration drops SinR below $7.5\,\mu$M, creating
a sharp but history-dependent gate.
Finally, the PreemptionCheck (\cref{eq:enablement}) couples commitment-layer
transitions through the signal hierarchy $\lambda$: when a Layer-A
transition is disabled, all transitions in the same cascade subtree are
jointly suppressed, introducing effective long-range correlations.

These three mechanisms --- sequential amplification, nonlinear inhibitor
gating, and PreemptionCheck coupling --- collectively suppress the
fluctuation amplitude near $\Nc$ relative to the mean-field prediction,
yielding $\gsub < 0.5$.
The deviation is large ($19\sigma$), confirming that the non-MF universality
class is a structural property of the commitment cascade, not a
finite-sample artefact.

The PreemptionCheck mechanism has a direct EPR signature that makes this
suppression transparent at the trajectory level.
When PreemptionCheck is false, the effective propensity of the gated
transition is zero and its EPR contribution is exactly zero: the cascade
enters a thermodynamic silence interval.
Because PreemptionCheck couples all Layer-A transitions through $\lambda$,
the silence is collective --- all downstream transitions go silent
simultaneously.
Near $N_c$, the cascade flickers rapidly between active (EPR burst) and
silent intervals before finally committing, generating correlated EPR
bursts rather than the independent Poisson events assumed by mean-field.
The variance of $\sigma[\omega]$ is thus suppressed relative to
$\langle\sigma[\omega]\rangle$: $\mathrm{Var}[\sigma]/\langle\sigma\rangle < 1$
near $N_c$, a direct thermodynamic fingerprint of the non-MF universality
class enforced by the cascade topology.

\subsection{The Markovian reservoir assumption: scope and limitations}

The subsystem thermodynamics framework (\Cref{sec:subsystem}) requires the
rest of the cell to act as a Markovian reservoir: no memory effects from
unmodelled processes may feed back into the commitment layer on the
commitment timescale ($\sim 300$~min).
The assumption is empirically supported by the Fujita validation:
if significant non-Markovian coupling existed (e.g., chromosome-replication
feedback modifying KinA autophosphorylation rates in a history-dependent
way), the three experimental constraints would not be reproducible
without explicitly modelling those processes.

The assumption has known limits.
During deep starvation, ATP depletion accelerates beyond the model's
constant-rate approximation, potentially introducing a positive feedback
(faster depletion $\to$ lower ATP $\to$ faster KinA activation $\to$
faster depletion) that violates the Markovian bath assumption.
Similarly, a starved-then-refed experiment (as in FWD perturbation, NET-T3)
produces a rapid metabolic response that is not captured by treating ATP
as a slowly varying reservoir.

A related concern is that the ATP pool is shared with all concurrent
cellular processes (translation, replication, membrane synthesis), not
dedicated to the sporulation subnetwork alone, so the $\sigma_{\rm interface}$
term in the decomposition \cref{eq:epr_decomp} is non-zero.
Three considerations bound this concern.
First, the signalling layer (KinA cycling) consumes only $\sim 8\,\mu$M ATP
over 6~h --- negligible relative to the 5000~$\mu$M pool.
Second, during the commitment window, starvation suppresses translation,
replication, and flagellar motors, reducing competing ATP drains
precisely when sporulation is active.
Third, and most importantly, the structural results ($N_c$, $\gsub$, EPR
decomposition ratios) are implicitly calibrated against the Fujita
experiments, which were performed in real cells with all concurrent
processes running.
The model reproduces those fractions without parameter tuning; the ATP
competition present in the biological experiment is therefore already
encoded in the effective parameter regime.
The absolute EPR values ($W/\kB T$, $\dot\sigma$ in $\kB T$~s$^{-1}$)
remain the most sensitive to the shared-bath assumption and should be
read as subnetwork-specific quantities, not total cellular dissipation.

The computed thermodynamic quantities are therefore valid for:
the commitment decision window (roughly from nutrient depletion to first
forespore formation, $\sim 200$--$360$~min depending on $N_0$);
under starvation conditions representative of the Fujita protocol;
and for the sporulation subnetwork specifically.
They do not represent the total cellular EPR (dominated by translation and
replication), nor the full-lifecycle thermodynamics from exponential growth
through spore germination.

\subsection{Topology encodes thermodynamics}

Three results converge on a single structural claim: the arc graph of
the commitment circuit contains the thermodynamic information about
how the decision will proceed.

$\etaL$ is topology-constant (\Cref{sec:results_eta}): the arc density
of the commitment layer $G_s$ does not change with kinetic parameters,
so the Landauer efficiency is determined by how many arcs connect how
many places and transitions --- not by rate magnitudes.

KinA dominating $97.5\%$ of the stochastic EPR (\Cref{sec:results_epr})
is a structural consequence: KinA is the sole input arc from the
nutrient reservoir to the commitment cascade.
All downstream thermodynamic flows pass through this single port.
No kinetic tuning can redistribute the EPR budget away from KinA
without rewiring the topology.

This structural EPR concentration connects directly to the
Thermodynamic Uncertainty Relation (TUR) of
Barato \& Seifert~\cite{Barato2015}:
\begin{equation}
  \frac{(\Delta J)^2}{\langle J \rangle^2} \geq \frac{2\kB}{\EPR}
  \label{eq:tur}
\end{equation}
A lower EPR implies lower precision of the output current $J$.
The bet-hedging window ($700$--$1800\,\mu$M, \Cref{fig:q3}) is precisely
the nutrient range in which the total stochastic EPR is low
($< 1\,\kB T$/s) and the TUR bound permits the cell to sacrifice
precision --- producing a broad $P(\mathrm{commit})$ distribution ---
in exchange for population-level diversity.
Bet-hedging is not a failure of precision; it is the thermodynamically
permissible operating regime at low EPR.

\subsection{Generality of the framework}

The five quantities ($\etaL$, $W/\kB T$, $\Nc$, $\gsub$, EPR decomposition)
are computable for any SHPN model that satisfies three conditions:
(i) known arc topology;
(ii) stochastic rates consistent with local detailed balance
(\cref{eq:ldb}); and
(iii) explicit chemical reservoirs (ATP/NTP pools, nutrient inputs).
No new experiments are required: the framework extracts thermodynamic
information from models already validated against existing biological data.

Direct candidate circuits:\begin{itemize}
  \item \textbf{\textit{Caulobacter crescentus} cell cycle} ---
    CtrA/DnaA phosphorelay controlling G1/S commitment; binary, irreversible,
    $\sim 10$ regulatory proteins at low copy number.
  \item \textbf{\textit{E.~coli} SOS response} --- LexA/RecA threshold
    circuit; commitment to DNA-damage repair or mutagenic bypass.
  \item \textbf{Mammalian apoptosis} --- Bcl-2 family inhibitor gate
    (analogous to SinR/SinI) controlling cytochrome~$c$ release;
    MOMP as the irreversible commitment point.
  \item \textbf{Haematopoietic lineage decisions} --- multi-attractor
    landscape; the $\kB T \ln|\mathcal{A}(\Ge)|$ Landauer scaling
    predicts EPR should increase with the number of accessible fates.
\end{itemize}
In each case, the SHPN arc structure is either known from literature or
inferable from ChIP-seq and interaction databases; the stochastic rates
are available from single-molecule measurements or FISH data.
The thermodynamic fingerprint --- which step concentrates the EPR, what
$\gsub$ is, where $\Nc$ lies --- is then computable without additional
experiments, providing a new layer of quantitative prediction from
existing validated models.

% ─────────────────────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclusion}
% ─────────────────────────────────────────────────────────────────────────────

We have shown that single-cell commitment decisions have a computable
thermodynamic characterisation within the NESM framework when modelled
with the SHPN formalism.
The arc semantics of the SHPN map directly onto the thermodynamic ports
of an open driven Markov system, and five quantities derivable from
topology and stochastic dynamics alone constitute a thermodynamic
fingerprint of the commitment circuit.

For \textit{B.~subtilis} sporulation, this fingerprint reveals:
the commitment transition is non-mean-field ($\gsub = 0.150 \pm 0.044$);
the critical nutrient threshold is $\Nc = 1346\,\mu$M;
the thermodynamic cost of commitment is overwhelmingly concentrated at
the sensing step, not the execution programme;
and the bet-hedging population window (700--1800\,$\mu$M) corresponds
to the regime where the Thermodynamic Uncertainty Relation allows
maximum population diversity at the cost of single-cell decision precision.

The Markovian reservoir assumption restricts the quantitative claims to
the decision window under starvation conditions.
Extending the framework to include metabolic dynamics and full-lifecycle
thermodynamics is a direct next step.
More broadly, the SHPN provides a ready substrate for computing the
thermodynamic fingerprint of any regulatory commitment circuit whose
topology and stochastic rates are known --- a new measurement handle on
the physics of cellular decision-making.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Acknowledgements}
% ─────────────────────────────────────────────────────────────────────────────

This work was performed within the SHyPN project at the Federal University
of Santa Catarina (UFSC), Brazil.
Computational resources were provided by a dedicated GPU server
(NVIDIA RTX~5060~Ti, CUDA~12.9).
The author thanks the Fujita and Losick laboratories for the published
experimental measurements that calibrated the \textit{B.~subtilis}
sporulation model.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Funding}
% ─────────────────────────────────────────────────────────────────────────────

This work received no external funding.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Author Contributions}
% ─────────────────────────────────────────────────────────────────────────────

\noindent E.S.: Conceptualization, Methodology, Software, Formal Analysis,
Investigation, Data Curation, Writing --- Original Draft,
Writing --- Review \& Editing, Visualization.

All work is that of the sole author.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Competing Interests}
% ─────────────────────────────────────────────────────────────────────────────

The author declares no competing financial or non-financial interests.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Data and Code Availability}
% ─────────────────────────────────────────────────────────────────────────────

The SHPN simulation engine and all model files (\texttt{.shy}) used in
this study are available at
\url{https://github.com/simao-eugenio/shypn-dev} under the MIT licence.
All sweep configurations, run provenance records (\texttt{provenance.json}),
and summary CSV files are archived in the same repository under
\texttt{workspace/projects/thesis/experiments/results/}.
Analysis scripts are in \texttt{workspace/projects/thesis/scripts/}.
Raw replicate trajectory data are available from the author on request.

% ─────────────────────────────────────────────────────────────────────────────
\section*{AI Disclosure}
% ─────────────────────────────────────────────────────────────────────────────

GitHub Copilot (Claude Sonnet~4.6, accessed via Visual Studio Code) was
used as a coding assistant during development of the SHPN simulation
engine and analysis scripts, and as a writing assistant during
manuscript drafting.
All scientific claims, mathematical derivations, simulation designs, and
interpretations of results are the sole intellectual work of the author.
No AI tool was used to generate or fabricate data, figures, or
quantitative results.
The author takes full responsibility for the accuracy and integrity of
all content.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Supplementary Material}
% ─────────────────────────────────────────────────────────────────────────────

\begin{description}
  \item[Supplementary~S1] Full 13-tuple Bio-PN firing semantics and
    proofs (enablement, firing rule, PreemptionCheck).
  \item[Supplementary~S2] Complete v9 model specification: 41-place,
    36-transition, 114-arc table with rate functions, initial markings,
    and arc types.
  \item[Supplementary~S3] Derivation of the $\etaL$ topology-constancy
    condition and its relation to gate co-operativity $n$.
  \item[Supplementary~S4] Proof that $W/\kB T \gg 1$ implies
    inapplicability of the Jarzynski equality and Crooks fluctuation
    theorem for commitment processes.
  \item[Supplementary~S5] Critical exponent fitting procedure:
    bootstrap uncertainty quantification for $\gsub$, sensitivity to
    $\Nc$ estimate, and comparison with v7 model exponent.
\end{description}

% ─────────────────────────────────────────────────────────────────────────────
%  REFERENCES  (placeholder — replace with BibTeX)
% ─────────────────────────────────────────────────────────────────────────────
\begin{thebibliography}{99}

\bibitem{Seifert2012}
  Seifert, U. (2012).
  Stochastic thermodynamics, fluctuation theorems and molecular machines.
  \textit{Reports on Progress in Physics}, \textbf{75}(12), 126001.

\bibitem{Seifert2005}
  Seifert, U. (2005).
  Entropy production along a stochastic trajectory and an integral
  fluctuation theorem.
  \textit{Physical Review Letters}, \textbf{95}, 040602.

\bibitem{Rao2018}
  Rao, R.\ \& Esposito, M. (2018).
  Conservation laws and work fluctuation relations in chemical reaction networks.
  \textit{New Journal of Physics}, \textbf{20}, 023007.

\bibitem{Esposito2010}
  Esposito, M.\ \& Van den Broeck, C. (2010).
  Three faces of the second law. I. Master equation formulation.
  \textit{Physical Review Letters}, \textbf{104}, 090601.

\bibitem{Barato2015}
  Barato, A.C.\ \& Seifert, U. (2015).
  Thermodynamic uncertainty relation for biomolecular processes.
  \textit{Physical Review Letters}, \textbf{114}, 158101.

\bibitem{Schnakenberg1976}
  Schnakenberg, J. (1976).
  Network theory of microscopic and macroscopic behavior of master equation
  systems.
  \textit{Reviews of Modern Physics}, \textbf{48}, 571.

\bibitem{Ajdari1992}
  Ajdari, A.\ \& Prost, J. (1992).
  Mouvement induit par un potentiel p\'{e}riodique de basse sym\'{e}trie:
  di\'{e}lectrophor\`{e}se puls\'{e}e.
  \textit{Comptes Rendus de l'Acad\'{e}mie des Sciences Paris II},
  \textbf{315}, 1635--1639.

\bibitem{Magnasco1993}
  Magnasco, M.O. (1993).
  Forced thermal ratchets.
  \textit{Physical Review Letters}, \textbf{71}, 1477.

\bibitem{Jarzynski1997}
  Jarzynski, C. (1997).
  Nonequilibrium equality for free energy differences.
  \textit{Physical Review Letters}, \textbf{78}, 2690.

\bibitem{Crooks1999}
  Crooks, G.E. (1999).
  Entropy production fluctuation theorem and the nonequilibrium work
  relation for free energy differences.
  \textit{Physical Review E}, \textbf{60}, 2721.

\bibitem{Liphardt2002}
  Liphardt, J., Dumont, S., Smith, S.B., Tinoco, I.\ \& Bustamante, C. (2002).
  Equilibrium information from nonequilibrium measurements in an experimental
  test of Jarzynski's equality.
  \textit{Science}, \textbf{296}, 1832--1835.

\bibitem{Wang2002}
  Wang, G.M., Sevick, E.M., Mittag, E., Searles, D.J.\ \& Evans, D.J. (2002).
  Experimental demonstration of violations of the second law of thermodynamics
  for small systems and short time scales.
  \textit{Physical Review Letters}, \textbf{89}, 050601.

\bibitem{Gillespie2001}
  Gillespie, D.T. (2001).
  Approximate accelerated stochastic simulation of chemically reacting systems.
  \textit{Journal of Chemical Physics}, \textbf{115}, 1716.

\bibitem{Hill1977}
  Hill, T.L. (1977).
  \textit{Free Energy Transduction in Biology.} Academic Press, New York.

\bibitem{Fujita2005}
  Fujita, M.\ \& Losick, R. (2005).
  Evidence that entry into sporulation in \textit{Bacillus subtilis} is
  governed by a gradual increase in the level and activity of the master
  regulator Spo0A.
  \textit{Genes \& Development}, \textbf{19}, 2236--2244.

\bibitem{Losick2008}
  Losick, R.\ \& Desplan, C. (2008).
  Stochasticity and cell fate.
  \textit{Science}, \textbf{320}, 65--68.

\bibitem{Simao2025}
  Sim\~{a}o, E. (2025).
  Signal Hierarchical Petri Nets for biological systems: formalism,
  engine, and thermodynamic extensions.
  [Preprint / manuscript in preparation]

\end{thebibliography}

\end{document}
