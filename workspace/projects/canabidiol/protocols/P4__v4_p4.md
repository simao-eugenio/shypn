# Protocol P4′ — Lock-in bifurcation map (single-bolus, fine LD scan)

> **Pairing.** Model: [`models/cbd_ad_neuroprotection_v4_p4.shy`](../models/cbd_ad_neuroprotection_v4_p4.shy)
> (sha256 `369ff0f9…`, 15 events, 138 145 bytes). Derived from v3 by
> removing `evt_maint_{1,2,3}` → pure single-bolus dispatch.

## Hypothesis

A bistable boundary exists between **clearing** and **locked** plaque
trajectories, located at a severity- and age-dependent critical
loading dose. Map it with refined LD resolution and enough replicates
per cell to classify each (Sev, LD, Age) cell as "reliably clears" /
"mixed" / "reliably locks".

## Origin

Refines finding **F1** in
[`docs/p1_deep_analysis_run_20260424_005438.md`](../docs/p1_deep_analysis_run_20260424_005438.md)
— Aβ-plaque lock-in detected in 93/1152 cells of P1 at LD ≥ 5.

## Environment panel — parameter places & events

| Place (ID) | Baseline | Units | Notes |
|---|---:|---|---|
| `Disease_Severity (P38)` | 2 | level | swept |
| `Age (P31)` | 75 | (signal) | swept |
| `pH (P29)` | 7.4 | (signal) | held constant |
| `LOADING_DOSE (P35)` | 5 | µM | swept (`{3,4,5,6,7}`) |
| `MAINT_DOSE (P36)` | 0 | µM | inert (no maintenance events in v4_p4) |
| `DOSE_INTERVAL (P37)` | 4300 | s | inert (no maintenance events) |

**Built-in events (15 total, inherited from v3 minus `evt_maint_*`):**

| id | trigger | role |
|---|---|---|
| `evt_install_*` ×14 | `t > 0.01` | install disease burden across 14 places |
| `evt_load`          | `t > 60`   | single CBD bolus at 1 min |

## Viability panel — sweep plan

**Mode:** Factorial • **Replicates:** 60 • **Duration:** 14400 s (4 h) • **Termination:** time

| Sweep parameter | Path | Values | Levels |
|---|---|---|---:|
| `[param] LOADING_DOSE` | `LOADING_DOSE.initial_marking` | 3, 4, 5, 6, 7 | 5 |
| `[param] Disease_Severity` | `Disease_Severity.initial_marking` | 1, 2, 3 | 3 |
| `[param] Age` | `Age.initial_marking` | 65, 75, 85 | 3 |

**Cells:** 5 × 3 × 3 = **45** • **Total sims:** 45 × 60 = **2700**

## Headless equivalent — `sweep_config.P4.json`

See [`../sweep_config.P4.json`](../sweep_config.P4.json).

## Dispatch

```bash
python -m shypn.cli.sweep \
    --project workspace/projects/canabidiol \
    --sweep   workspace/projects/canabidiol/sweep_config.P4.json \
    --workers 4 --verbose
```

## Results — `run_20260424_165603`

2760/2760 OK (~80 min wall total). Corroborates P1 findings F2/F5/F6/F7;
F4 variance partition shifts (Age now 92.2 % vs LD 50 % in P1) because
P4 dropped LD=0 — an *artefact of restricting the design window*, not
a contradiction. F1 bimodality not seen because the basin boundary
likely sits outside `LD ∈ [3, 7]`. New finding: at clinical-realistic
LD ≤ 7, Age-85 stuck at NH ≈ 70.

Detailed comparison: see `docs/p4_vs_p1_comparison.md` (TBD).
