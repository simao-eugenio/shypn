> ⚠ **PARKED — model never built; pre-Pattern-A pairing in spec.**
> Re-spec against `v3_p8` derivative. See
> `docs/recon_post_engine_fix_2026-04-27.md`.

# Protocol P3 — Late rescue (post-onset window)

> **Pairing.** Model: TBD — requires a `RESCUE_DELAY (P??)` parameter
> place not present in the current `cbd_ad_neuroprotection_v3.shy`.
> Two implementation paths:
>
> 1. Add `RESCUE_DELAY` to v3 → `cbd_ad_neuroprotection_v3_p3.shy`
>    and define `evt_rescue` / `evt_maint` triggered on it.
> 2. Run one `sweep_config.P3.<delay>.json` per literal delay value
>    (5 separate dispatches).
>
> Approach (1) is cleaner; (2) is a stop-gap. **Decision pending.**

## Hypothesis

Treatment started *after* inflammation onset still recovers neuron
health if begun before plaque crosses ~30. Establishes the
**therapeutic time window** as a function of disease severity.

## Environment panel — parameter places & events

| Place (ID) | Baseline | Units | Notes |
|---|---:|---|---|
| `Disease_Severity (P38)` | 2 | level | swept |
| `RESCUE_DELAY (P??)` | 3600 | s | swept (the protocol's headline axis) |
| `LOADING_DOSE (P35)` | 10 | µM | held constant |
| `MAINT_DOSE (P36)` | 3 | µM | held constant |
| `DOSE_INTERVAL (P37)` | 4300 | s | held constant |
| `Age (P31)` | 75 | (signal) | swept |
| `pH (P29)` | 7.4 | (signal) | held constant |
| `CBD_extracellular (P1)` | 0 | µM | reset by `evt_rescue` |

**Sweep-injected events:**

| id | trigger | target | expression |
|---|---|---|---|
| `evt_rescue` | `t > RESCUE_DELAY` | `CBD_extracellular` | `+ LOADING_DOSE` |
| `evt_maint`  | `t > RESCUE_DELAY + DOSE_INTERVAL` | `CBD_extracellular` | `+ MAINT_DOSE` |

> ⚠ The model's built-in `evt_load` (at `t > 0.1`) and
> `evt_maint_{1,2,3}` must be **suppressed** for P3, otherwise the
> "late rescue" arm would actually receive an early bolus too. This
> requires a `_p3` model variant.

## Viability panel — sweep plan

**Mode:** Factorial • **Replicates:** 30 • **Duration:** 14400 s (4 h) • **Termination:** time

| Sweep parameter | Path | Values | Levels |
|---|---|---|---:|
| `[param] Disease_Severity` | `Disease_Severity.initial_marking` | 1, 2, 3 | 3 |
| `[param] RESCUE_DELAY` | `RESCUE_DELAY.initial_marking` | 1800, 3600, 5400, 7200, 9000 | 5 |
| `[param] Age` | `Age.initial_marking` | 65, 75, 85 | 3 |

**Cells:** 3 × 5 × 3 = **45** • **Total sims:** 45 × 30 = **1350**

## Headless equivalent — `sweep_config.P3.json`

```json
{
  "mode": "factorial",
  "model_path": "workspace/projects/canabidiol/models/cbd_ad_neuroprotection_v3_p3.shy",
  "replicates": 30,
  "duration": 14400,
  "termination": "time",
  "seed_base": 42,
  "tau_epsilon": 0.03,
  "max_tau": 0.1,
  "parameters": [
    {"type": "places", "path": "Disease_Severity.initial_marking", "values": [1, 2, 3]},
    {"type": "places", "path": "RESCUE_DELAY.initial_marking",     "values": [1800, 3600, 5400, 7200, 9000]},
    {"type": "places", "path": "LOADING_DOSE.initial_marking",     "values": [10]},
    {"type": "places", "path": "MAINT_DOSE.initial_marking",       "values": [3]},
    {"type": "places", "path": "DOSE_INTERVAL.initial_marking",    "values": [4300]},
    {"type": "places", "path": "Age.initial_marking",              "values": [65, 75, 85]}
  ],
  "events": [
    {"id": "evt_rescue", "trigger": "t > RESCUE_DELAY",                 "assignments": {"CBD_extracellular": "CBD_extracellular + LOADING_DOSE"}},
    {"id": "evt_maint",  "trigger": "t > RESCUE_DELAY + DOSE_INTERVAL", "assignments": {"CBD_extracellular": "CBD_extracellular + MAINT_DOSE"}}
  ]
}
```

## Status

⏸ Not yet dispatched. Pre-requisites:
1. Add `RESCUE_DELAY` parameter place to model.
2. Create `cbd_ad_neuroprotection_v3_p3.shy` with built-in `evt_load`
   and `evt_maint_{1,2,3}` removed (so only the late `evt_rescue`
   delivers drug).
